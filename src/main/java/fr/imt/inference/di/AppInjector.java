package fr.imt.inference.di;

import com.google.inject.AbstractModule;

public class AppInjector extends AbstractModule {

    @Override
    protected void configure() {
        // bind(ExpressionFactory.class);
        // bind(ConstraintRepository.class);
    }
}


